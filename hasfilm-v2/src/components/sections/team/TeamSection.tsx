import React from 'react';

const TeamSection: React.FC = () => {
    return (
        <section style={{ height: '150vh', display: 'flex', alignItems: 'center', justifyContent: 'center', borderBottom: '1px solid #333' }}>
            <h2>Team Section</h2>
        </section>
    );
};

export default TeamSection;
