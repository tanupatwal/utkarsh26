import React from 'react';

const ScheduleSection: React.FC = () => {
    return (
        <section style={{ height: '100vh', display: 'flex', alignItems: 'center', justifyContent: 'center', borderBottom: '1px solid #333' }}>
            <h2>Schedule Section</h2>
        </section>
    );
};

export default ScheduleSection;
