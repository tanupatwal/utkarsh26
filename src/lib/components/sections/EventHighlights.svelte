<script lang="ts">
</script>

<section class="section" style="height: 150dvh;" data-zone="HIGHLIGHTS">
    <div class="highlights-sticky">
        <h2 class="highlights-title">EVENT HIGHLIGHTS</h2>
        <p class="highlights-sub">
            Placeholder — 12-image bento gallery with parallax
        </p>
    </div>
</section>

<style>
    .highlights-sticky {
        position: sticky;
        top: 0;
        height: 100dvh;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        padding: var(--section-padding);
    }

    .highlights-title {
        font-family: var(--font-display);
        font-size: clamp(2rem, 5vw, 3.5rem);
        font-weight: 800;
        letter-spacing: 0.1em;
    }

    .highlights-sub {
        font-family: var(--font-mono);
        font-size: 0.75rem;
        color: var(--color-muted);
        margin-top: 1rem;
    }
</style>
